# 贡献指南

感谢参与。本项目是**对官方客户端协议的实测还原**，因此最有价值的贡献往往是"这个结论过期了"或"这条实测是错的"。

## 开发环境

```bash
npm install
npm test          # 单元测试（vitest）
npm start         # 本地起网关 :19950
```

需要本机至少有一个已登录的 Trae 客户端（国内版或国际版均可），网关会读取其凭证。

## 提交前

```bash
npm test
```

测试全部通过即可提交。改动涉及协议行为时，请在 PR 描述里附上**实测证据**（请求/响应片段、错误码、观测日期）。

## 代码约定

- **区域知识集中放在 `src/realms.js`**：host、默认版本、模型归属、区域判定规则都写在这里。不要在业务代码里散落 `if (realm === 'cn')`——区域差异一旦分散就很难审计，而错路由的报错信息通常不会提到区域。
- **凭证的归属由凭证自身决定**（见 `realms.realmFromCredential`），不要用"当前选中区域"去反推账号属于哪个区域。
- **注释解释"为什么"，不解释"是什么"**。协议代码里的注释应说明观测到的行为与陷阱（这类知识无法从代码本身读出），而不是复述代码逻辑。
- 错误码语义、上游怪癖等结论请标注观测日期，方便后来者判断时效。

## 敏感信息

提交前请确认**没有**带入：

- `accounts/` 下的任何真实凭证（该目录已被 `.gitignore` 排除）
- `.env`（只提交 `.env.example`）
- `logs/`、`captures/`、`output/` 等运行时产物
- 真实账号 ID、邮箱、内网域名、个人绝对路径

CI 里有一个 hygiene 检查会拦截疑似凭证，但请勿依赖它兜底。

## 新增模型或区域

模型清单来自官方客户端，改动时请：

1. 用最小请求实测确认该模型在当前区域**真的可调用**（不要只依据客户端界面显示）；
2. 更新 `src/realms.js` 的 `REALM_EXCLUSIVE_MODELS`（若涉及区域归属）；
3. 在 `docs/PROTOCOL.md` 记录实测结论与日期。

## 报告问题

请附上：

- `GET /v1/status` 与 `GET /v1/pool` 的输出（**先确认其中不含可用的 token**）
- 复现所需的请求体（模型名、参数）
- 上游返回的错误码与消息

**切勿在 issue 里粘贴可用凭证。**
